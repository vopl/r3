/**
  * @name QByteArrayMatcher
  * @constructor
  */
function QByteArrayMatcher(){return undefined;};

/**
  * @name QByteArrayMatcher^1
  * @constructor
  * @param {QByteArray} pattern
  */
function QByteArrayMatcher(pattern){return undefined;};

/**
  * @name QByteArrayMatcher^2
  * @constructor
  * @param {QByteArrayMatcher} other
  */
function QByteArrayMatcher(other){return undefined;};

/**
  * @name QByteArrayMatcher^3
  * @constructor
  * @param {String} pattern
  * @param {Numeric} length
  */
function QByteArrayMatcher(pattern, length){return undefined;};

/**
  * @name indexIn
  * @function
  * @memberOf QByteArrayMatcher.prototype
  * @param {QByteArray} ba
  * @param {Numeric} from
  * @returns {Numeric}
  */
QByteArrayMatcher.prototype.indexIn = function(ba, from){return new Numeric();};

/**
  * @name pattern
  * @function
  * @memberOf QByteArrayMatcher.prototype
  * @returns {QByteArray}
  */
QByteArrayMatcher.prototype.pattern = function(){return new QByteArray();};

/**
  * @name setPattern
  * @function
  * @memberOf QByteArrayMatcher.prototype
  * @param {QByteArray} pattern
  * @returns {undefined}
  */
QByteArrayMatcher.prototype.setPattern = function(pattern){return undefined;};

